{
  "stage": "production",
  "workflow": "post-approval-prototype",
  "actor": "ai_confirmed",
  "status": "IMPLEMENTED — native workflow with pinned coordinator inputs and real browser checks",
  "purpose": "After concept approval: portable HTML product prototype and all-screen/state inspection board; no Figma publication.",
  "inputContract": {
    "required": [
      "inputPath",
      "outDir"
    ],
    "notes": "product/integration.mjs prepares a revision-pinned input from coordinator approvals before invoking the built-in native workflow."
  },
  "inputExample": {
    "inputPath": "/abs/integration/runs/run-demo/production/input.json",
    "outDir": "/abs/integration/runs/run-demo/production/output"
  },
  "outputContract": {
    "required": [
      "stage", "status", "inputPath", "outDir", "humanFinalInspection", "figma"
    ],
    "notes": "The coordinator records completion only with a matching ready-for-review handoff and browser evidence. This is not human approval."
  },
  "outputExample": {"stage":"post-approval-prototype","status":"ready-for-review","inputPath":"/abs/input.json","outDir":"/abs/output","humanFinalInspection":true,"figma":false},
  "handoff": {
    "to": null,
    "via": "End of the currently implemented pipeline."
  }
}
